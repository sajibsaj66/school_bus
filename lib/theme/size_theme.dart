import 'package:flutter/material.dart';

class SizeTheme {
  static const double borderRadiusSize = 12.0;

  static const double paddingLargeSize = 16.0;
  static const double paddingMiddleSize = 8.0;
  static const double paddingSmallSize = 4.0;
  static const double paddingMiniSize = 2.0;

  static const double iconMainSize = 28.0;
  static const double iconMiddleSize = 24.0;
  static const double iconSmallSize = 20.0;

  static const double textLargeSize = 24.0;
  static const double textMiddleSize = 17.0;
  static const double textSmallSize = 14.0;
  static const double textMiniSize = 10.5;
  static const double textHeight = 1.4;

  static const Size outlinedButtonSize = Size.fromHeight(textLargeSize * 2);
  static const Size outlinedWrapButtonSize = Size(textLargeSize * 2, textLargeSize * 2);
  static const Size outlinedSmallButtonSize = Size(textMiddleSize, textMiddleSize);
  static const Size textButtonSize = Size.fromHeight(textSmallSize);
}